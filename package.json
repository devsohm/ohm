{
  "name": "ohm-workspace",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/devsohm/ohm.git"
  },
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "npm run clean --workspaces --if-present",
    "build": "npm run build --workspace @ohm/terminal && npm run build:offline --workspace @ohm/models && npm run build --workspace @ohm/kernel && npm run build --workspace ohm",
    "check": "npm run lint && npm run check:dependencies && npm run check:workspaces && npm run build && npm run check --workspace @ohm/terminal && npm run check --workspace @ohm/models && npm run check --workspace @ohm/kernel && npm run check --workspace ohm && npm run check:nonworkspace",
    "check:dependencies": "node scripts/check-dependency-policy.mjs",
    "check:nonworkspace": "npm test --prefix packages/ohm/examples/mcp-stdio && npm test --prefix packages/ohm/examples/starter && npm test --prefix packages/ohm/examples/subagent-specialists",
    "check:workspaces": "node scripts/check-workspace-boundaries.mjs",
    "install:user": "node packages/ohm/scripts/install-user.mjs",
    "lint": "oxlint . --deny-warnings --report-unused-disable-directives",
    "update:user": "node packages/ohm/scripts/update-user.mjs",
    "uninstall:user": "node packages/ohm/scripts/uninstall-user.mjs --yes",
    "benchmark:release-offline": "npm run benchmark:release-offline --workspace ohm",
    "benchmark:runtime": "npm run benchmark:runtime --workspace ohm",
    "release:stage": "node scripts/stage-release.mjs",
    "release:source": "node scripts/source-archive.mjs",
    "release:standalone": "node scripts/build-standalone.mjs",
    "release:finalize": "node scripts/finalize-release.mjs",
    "release:verify": "node scripts/verify-release-artifact.mjs",
    "test": "npm test --workspaces --if-present",
    "test:coverage:risk": "npm run build --workspace @ohm/terminal && npm run build:offline --workspace @ohm/models && npm run build --workspace @ohm/kernel && npm run build --workspace ohm && npm run test:coverage:risk --workspace ohm",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=26.7.0"
  },
  "allowScripts": {
    "@google/genai": false,
    "esbuild": false,
    "fsevents": false,
    "protobufjs": false
  },
  "devDependencies": {
    "@types/node": "26.2.0",
    "c8": "12.0.0",
    "monocart-coverage-reports": "2.13.0",
    "oxlint": "1.80.0",
    "tsx": "4.23.12",
    "typescript": "7.0.2"
  }
}
